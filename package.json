{
  "name": "farm-management",
  "version": "1.0.0",
  "scripts": {
    "build": "curl -L https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.16.0-stable.tar.xz | tar xJ && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter doctor && flutter pub get && flutter build web --release"
  }
}
